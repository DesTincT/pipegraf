# maxgraf compatibility shim

`maxgraf` is deprecated and now forwards to `pipegraf`.

This package exists only for backward compatibility with existing imports.

- Runtime exports are re-exported from `pipegraf`.
- Use `pipegraf` directly for new integrations.
